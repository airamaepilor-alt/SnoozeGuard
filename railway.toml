[build]
buildCommand = "npm run build --workspace=@snoozeguard/shared && npm run build --workspace=@snoozeguard/api"

[deploy]
startCommand = "npm run start --workspace=@snoozeguard/api"

[variables]
RAILPACK_INSTALL_CMD = "npm install --ignore-scripts --no-audit"
